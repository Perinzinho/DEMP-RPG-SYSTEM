Estrutura de pastas FrontEnd e sua utilidades-Temporário!!!!
 ---src/
├── assets/              → imagens, ícones, fontes
├── components/          → componentes reutilizáveis (Button, Input, Modal, Card)
├── pages/                → telas da aplicação (Login, Dashboard, RoomPage, CharacterSheet)
├── hooks/                → hooks customizados (useAuth, useFetch)
├── services/             → chamadas à API (api.js, authService.js, roomService.js)
├── contexts/             → Context API (AuthContext, RoomContext)
├── routes/               → configuração de rotas (PrivateRoute, AppRoutes)
├── utils/                → funções auxiliares (formatters, validators)
├── styles/               → estilos globais
├── App.jsx
└── main.jsx